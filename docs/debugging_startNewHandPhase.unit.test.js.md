Based on the test output, the hang occurs after several tests have passed, specifically after the test checking the DEALING phase (`should handle new hand from DEALING phase (e.g. misdeal recovery)`), and before the test `should deal to disconnected but active players` completes. The logs `[getNextPlayer Call #X]` and `Shuffling deck with 24 cards` are the last output before the hang, indicating the code has progressed past deck creation and dealing into the player turn/bidding setup logic.

Here are 10 probable reasons for the hang, directly related to the interaction between the `startNewHand` logic and its mocked dependencies in this specific test file:

1.  **Incorrect Mocking of `getNextPlayer` for Sequential Calls:** The `startNewHand` function internally calls `getNextPlayer` multiple times (once to determine the new dealer, then repeatedly *inside a `while` loop* to find the first active bidder). The hanging tests mock `mockPlayerUtils.getNextPlayer` using `.returns()`. This stub configuration causes the mock to **always return the same single player role** on every call.
2.  **Infinite Loop in First Bidder Determination:** The `startNewHand` logic includes a `while (!shouldDealToPlayer(firstBidder))` loop to find the first active player after the dealer. This loop repeatedly calls the mocked `getNextPlayer(firstBidder, PLAYER_ROLES)` to find the next candidate for `firstBidder`.
3.  **`firstBidder` Variable Never Updates:** Because the mocked `getNextPlayer` in these tests *always returns the same player role*, the `firstBidder` variable *never actually changes* inside the `while` loop body after its initial assignment.
4.  **Loop Condition Becomes Statically True:** If the player initially determined as `firstBidder` (the player immediately left of the dealer, calculated *before* the `while` loop) does *not* satisfy the `shouldDealToPlayer` condition, the loop condition `!shouldDealToPlayer(firstBidder)` becomes `!false`, which is `true`.
5.  **Stuck in `while (true)`:** Since `firstBidder` never changes (due to the incorrect mock) and `shouldDealToPlayer(firstBidder)` evaluates to `false` on every iteration, the `while (!false)` loop is equivalent to `while (true)` and becomes an infinite loop.
6.  **`shouldDealToPlayer` Evaluation:** The `shouldDealToPlayer` function checks `player.isActive !== false && player.isConnected !== false`. In the hanging tests (e.g., `"should deal to disconnected but active players"`), player statuses are manipulated. Depending on the dealer's position and which player is initially chosen as `firstBidder`, that player might be one where `shouldDealToPlayer` returns `false` (e.g., an "inactive" player in test 3). This `false` result triggers the infinite loop.
7.  **Safety Check Not Reached:** The `while` loop in `startNewHand` has a safety check `if (firstBidder === newDealer) throw new PhaseLogicError(...)`. This check is designed to prevent an infinite loop *if no player is active*. However, it is *not* triggered if `getNextPlayer` *always* returns the same player role that is *not* the new dealer, even if that player is inactive. The loop simply gets stuck before hitting this check.
8.  **Incorrect Mock Contract:** The test mocks the `getNextPlayer` utility function. The contract of `getNextPlayer` is to receive a player role and return the *next* player role in sequence. The mock `.returns()` violates this contract by always returning the *same* output regardless of the input, which breaks the logic of the code that relies on the dependency's sequential behavior.
9.  **Contrast with Working Tests:** The tests in the `GAME_OVER Phase Handling` block successfully mock `getNextPlayer` using `onFirstCall()` and `onSecondCall()` to simulate the *sequential* behavior needed by `startNewHand`. This demonstrates that mocking `getNextPlayer` correctly is crucial and confirms the issue isn't with `startNewHand`'s logic itself, but how its dependency (`getNextPlayer`) is being mocked in the hanging tests.
10. **Test Filtering Issues:** While not the cause of the hang itself, the `npm test ... -t "test title"` syntax being interpreted incorrectly by npm/Mocha (as shown by the warning `Warning: Cannot find any files matching pattern "should deal to disconnected but active players"`) means *all* tests in the file are being run, including the failing/hanging ones. This prevents isolating the problematic test for easier debugging using the debugger. (Note: This doesn't *cause* the hang, but hinders fixing it).

**In Summary:** The hang is overwhelmingly likely due to the tests incorrectly mocking the `getNextPlayer` utility function. They should use `.callsFake()` or sequential stubs (`.onFirstCall()...`) to simulate the cycling behavior required by the code under test, rather than `.returns()` which provides a static value causing an infinite loop in the player determination logic.